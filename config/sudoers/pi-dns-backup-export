# Pi backup export wrapper NOPASSWD rule
# Managed in: /etc/sudoers.d/pi-dns-backup-export
Defaults:pi !requiretty
Defaults:pi !use_pty
pi ALL=(root) NOPASSWD: /usr/local/sbin/infra-backup-dns-export
