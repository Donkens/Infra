# CODEX.md

Follow `./AGENTS.md` only.
If any repository instruction conflicts, `./AGENTS.md` wins.
No policy is defined here.

## Host verification reminder

Before any audit or write: read `AGENTS.md § HOST VERIFICATION`.
Verify `hostname`, `whoami`, `echo "$HOME"`, and `~/.machine-identity` before assuming which host or user you are operating as.
